# GitHub Pages

This package is a library. It is not deployed as a site.

Learner hubs that consume it must publish a Vite static `dist/` with:

- no Node runtime
- no server framework
- Core theme CSS bundled or copied
- curriculum JSON as static assets
