import type { ReactNode } from "react";
export type LearningOutcomeBadgeProps = {
    id?: string;
    title?: string;
};
export declare function LearningOutcomeBadge({ id, title }: LearningOutcomeBadgeProps): ReactNode;
